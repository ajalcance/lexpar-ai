# Agents worker dependencies.
# The LLM / STT / TTS pipeline deps (livekit-agents, provider SDKs) are added once API keys
# exist. For now this covers the no-key modules (session_state, verification) and their tests.

pytest>=8.0
ruff>=0.6
