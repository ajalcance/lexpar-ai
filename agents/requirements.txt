# Agents worker dependencies.
# Fireworks (and self-hosted vLLM later) are OpenAI-compatible, so we use the openai client.
# STT/TTS pipeline deps (livekit-agents, Deepgram, ElevenLabs) are added once those keys exist.

openai>=1.40
python-dotenv>=1.0
httpx>=0.27

# Dev / CI
pytest>=8.0
ruff>=0.6
