# Runtime + dev dependencies for the LexPar AI backend.
# Single file so CI's `pip install -r requirements.txt` covers lint + test too.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3
psycopg2-binary>=2.9
alembic>=1.13
PyJWT>=2.8

# Dev / CI
pytest>=8.0
httpx>=0.27
ruff>=0.6
