.DS_Store
.env
# Droplet production env (real secrets; .env.prod.example documents the shape)
.env.prod
node_modules/
__pycache__/
*.pyc
dist/
build/

# Python / backend
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.db
*.egg-info/

# Operator-supplied official court-rule documents (never committed - see scripts/seed_court.py)
seed_data/

# SSH keypairs / private keys — NEVER commit (deploy keys live outside the repo)
lexpar
lexpar.pub
id_rsa*
id_ed25519*
*.pem

# Transient worker/run logs (runtime artifacts, not source)
worker.log
*.log
